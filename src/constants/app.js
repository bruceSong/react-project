/**
 *  用于定义 app 层 常量
 *  serviceBaseUrl -  service  api  base url
 *  serviceVersion -  service 的版本号
 *  servicePrefix - service 地址 前缀
 */
const APP = {
   serviceBaseUrl : '',
   serviceVersion : 'v1',
   servicePrefix : ''
};
export default APP;
